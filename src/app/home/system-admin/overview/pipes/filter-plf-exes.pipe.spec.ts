import { FilterPlfExesPipe } from './filter-plf-exes.pipe';

describe('FilterPlfExesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlfExesPipe();
    expect(pipe).toBeTruthy();
  });
});
