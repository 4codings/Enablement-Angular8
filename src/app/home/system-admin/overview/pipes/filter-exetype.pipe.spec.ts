import { FilterExetypePipe } from './filter-exetype.pipe';

describe('FilterExetypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExetypePipe();
    expect(pipe).toBeTruthy();
  });
});
