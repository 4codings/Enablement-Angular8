import { FilterMachinesPipe } from './filter-machines.pipe';

describe('FilterMachinesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMachinesPipe();
    expect(pipe).toBeTruthy();
  });
});
